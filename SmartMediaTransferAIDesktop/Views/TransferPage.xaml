<Page x:Class="SmartMediaTransferAIDesktop.Views.TransferPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="TransferPage"
      Foreground="White">

    <Grid>
        <!-- État A : En attente -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <Grid Margin="0,0,0,48" Width="400" Height="200">
                <ui:SymbolIcon Symbol="Phone24" FontSize="80" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="#8C90A6" Opacity="0.5"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ui:ProgressRing IsIndeterminate="True" Width="48" Height="48" Foreground="#0077FF" />
                </StackPanel>

                <ui:SymbolIcon Symbol="Desktop24" FontSize="100" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#FFFFFF"/>

                <skia:SKElement x:Name="LottiePlayer" PaintSurface="LottiePlayer_PaintSurface" IgnorePixelScaling="True" />
            </Grid>

            <TextBlock Text="En attente de connexion..." FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#FFFFFF" Margin="0,0,0,16"/>
            <TextBlock Text="Scannez ce QR Code avec Smart Media Transfer sur votre mobile." FontSize="16" Foreground="#8C90A6" HorizontalAlignment="Center" Margin="0,0,0,32"/>

            <!-- Fake QR Code Placeholder -->
            <Border Width="150" Height="150" Background="#FFFFFF" HorizontalAlignment="Center" CornerRadius="8" Padding="8">
                <ui:SymbolIcon Symbol="QrCode24" FontSize="100" Foreground="#0F111A" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </StackPanel>
    </Grid>
</Page>
