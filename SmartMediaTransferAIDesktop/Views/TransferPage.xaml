<Page x:Class="SmartMediaTransferAIDesktop.Views.TransferPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="TransferPage"
      Foreground="White">

    <Grid>
        <!-- Background Gradient Simulation -->
        <Border>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0F111A" Offset="0"/>
                    <GradientStop Color="#1a142c" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <!-- Isometric/3D representation stub (Using icons for now, Lottie in production) -->
            <Grid Margin="0,0,0,48" Width="400" Height="200">
                <ui:SymbolIcon Symbol="Phone24" FontSize="80" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="#A0A0A0"/>

                <!-- Animated particles stub -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ui:SymbolIcon Symbol="ArrowRight24" FontSize="32" Foreground="{StaticResource PrimaryBrush}" Margin="8"/>
                    <ui:SymbolIcon Symbol="ArrowRight24" FontSize="32" Foreground="{StaticResource SecondaryBrush}" Margin="8"/>
                    <ui:SymbolIcon Symbol="ArrowRight24" FontSize="32" Foreground="#06B6D4" Margin="8"/>
                </StackPanel>

                <ui:SymbolIcon Symbol="Desktop24" FontSize="100" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="White"/>
            </Grid>

            <TextBlock Text="Waiting for connection..." FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="Scan the QR code from the Android app to begin." FontSize="16" Foreground="#A0A0A0" HorizontalAlignment="Center" Margin="0,0,0,32"/>

            <ui:ProgressRing IsIndeterminate="True" Width="48" Height="48" Foreground="{StaticResource PrimaryBrush}" />
        </StackPanel>
    </Grid>
</Page>
