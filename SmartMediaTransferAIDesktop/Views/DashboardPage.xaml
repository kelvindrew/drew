<Page x:Class="SmartMediaTransferAIDesktop.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="DashboardPage"
      Foreground="White">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Welcome back," FontSize="18" Foreground="#A0A0A0" />
                <TextBlock Text="Overview" FontSize="36" FontWeight="Bold" />
            </StackPanel>

            <!-- Top Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Storage Card -->
                <ui:Card Grid.Column="0" Padding="24">
                    <StackPanel>
                        <ui:SymbolIcon Symbol="Database24" FontSize="28" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Left" Margin="0,0,0,16"/>
                        <TextBlock Text="Storage Used" FontSize="16" Foreground="#A0A0A0" Margin="0,0,0,8" />
                        <TextBlock Text="145.2 GB" FontSize="28" FontWeight="SemiBold" />
                        <ProgressBar Value="65" Height="4" Margin="0,16,0,0" Foreground="{StaticResource PrimaryBrush}" />
                    </StackPanel>
                </ui:Card>

                <!-- Files Card -->
                <ui:Card Grid.Column="2" Padding="24">
                    <StackPanel>
                        <ui:SymbolIcon Symbol="Document24" FontSize="28" Foreground="{StaticResource SecondaryBrush}" HorizontalAlignment="Left" Margin="0,0,0,16"/>
                        <TextBlock Text="Total Files" FontSize="16" Foreground="#A0A0A0" Margin="0,0,0,8" />
                        <TextBlock Text="12,408" FontSize="28" FontWeight="SemiBold" />
                        <TextBlock Text="+234 this week" FontSize="12" Foreground="{StaticResource SuccessBrush}" Margin="0,16,0,0" />
                    </StackPanel>
                </ui:Card>

                <!-- Devices Card -->
                <ui:Card Grid.Column="4" Padding="24">
                    <StackPanel>
                        <ui:SymbolIcon Symbol="Phone24" FontSize="28" Foreground="#06B6D4" HorizontalAlignment="Left" Margin="0,0,0,16"/>
                        <TextBlock Text="Trusted Devices" FontSize="16" Foreground="#A0A0A0" Margin="0,0,0,8" />
                        <TextBlock Text="3" FontSize="28" FontWeight="SemiBold" />
                        <TextBlock Text="All connected" FontSize="12" Foreground="{StaticResource SuccessBrush}" Margin="0,16,0,0" />
                    </StackPanel>
                </ui:Card>
            </Grid>

            <!-- Bottom Section -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <ui:Card Grid.Column="0" Padding="24">
                    <StackPanel>
                        <TextBlock Text="Recent Transfers" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>
                        <!-- Placeholder for List -->
                        <TextBlock Text="No active transfers" Foreground="#808080" HorizontalAlignment="Center" Margin="0,48,0,0"/>
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="2" Padding="24">
                    <StackPanel>
                        <TextBlock Text="AI Suggestions" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>
                        <!-- Placeholder for AI -->
                        <ui:SymbolIcon Symbol="Sparkle24" FontSize="48" Foreground="{StaticResource SecondaryBrush}" HorizontalAlignment="Center" Margin="0,32,0,16"/>
                        <TextBlock Text="Your library is perfectly organized." Foreground="#A0A0A0" TextWrapping="Wrap" TextAlignment="Center" />
                    </StackPanel>
                </ui:Card>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
